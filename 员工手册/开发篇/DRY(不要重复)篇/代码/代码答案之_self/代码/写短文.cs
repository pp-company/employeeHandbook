using System;
using System.Collections.Generic;
using System.Text;

namespace 代码
{
    class 写短文 : 可以写的
    {
        protected 可以写的 _可以写的;
        public 写短文(可以写的 可以写的)
        {
            _可以写的 = 可以写的;
        }
        public void 写(string 填写文字)
        {
            _可以写的.写(填写文字);
        }
        public void 署名()
        {
            写("名字");
            Console.WriteLine("盖章");
            Console.WriteLine("日期");
        }
    }
}
